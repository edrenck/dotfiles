# completions.zsh — Manual completions/extensions (modular Zsh)
#
# For advanced/rare cases where a CLI tool provides its own Zsh completion script
# and it is not yet available via plugins or env.zsh.
# Most users will not need to modify this file.
#
# Loading here ensures that plugins and theme are loaded first, respecting the load order for performance.

# zoxide completions and command/hook setup (for when zoxide is available; safe if missing)

# (Other manual completions can follow below)
# source "/usr/local/share/myspecialtool/completion.zsh"
# Additional completions may be added here as needed.
