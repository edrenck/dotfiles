# plugins.zsh — Antidote plugin specification (for static loader)
# List of plugins for Antidote to install. One per line, comment above or inline as needed.

romkatv/powerlevel10k            # Modern theme with icons, palette, VCS
zsh-users/zsh-autosuggestions    # Fish-like autosuggest CLI UX
zsh-users/zsh-syntax-highlighting # Real-time syntax and error highlighting
#Aloxaf/fzf-tab                   # Fuzzy, Fish-style tab completion (now sourced manually per loader bug)
zsh-users/zsh-completions        # Rich extra completions
ajeetdsouza/zoxide               # Fastest jump tool, replaces cd for power users
jeffreytse/zsh-vi-mode           # Vi keybindings, minimal
# (Add more as desired, but keep it lean for startup performance)
